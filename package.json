{
  "name": "chatify",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client:build": "cd client && npm install && ng build --configuration production",
    "server:start": "cd server && npm install && node server.js",
    "start": "npm run client:build && npm run server:start"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}